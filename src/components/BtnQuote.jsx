import React from 'react'

export const BtnQuote = ({objStyleBtn, handleClick}) => {

  
  return (
    <button style={objStyleBtn} className='card__btn' onClick={handleClick} >&gt;</button>
    
    
  )
}
